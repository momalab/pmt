#pragma once

#include <string>

#include "sz.tmp"

namespace cons
{

const std::string configfile = "bloomfilter.cfg.tmp";
const std::string filterfile = "bloomfilter.db.tmp";
const std::string localqueryfile = "localquery.tmp";
const std::string queryfile = "query.tmp";
const std::string randomfile = "random.tmp";
const std::string erandomfile = "erandom.tmp";
const std::string randomfilterfile = "randombloomfilter.db.tmp";
const std::string resultfile = "result.tmp";
const char delimiter = ' ';

} // cons
